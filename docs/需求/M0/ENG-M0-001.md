下面这版可以直接复制给你的 SDD / 开发 Agent。它把 **M0-001 作为一个完整 Change**，内部再拆 Task / Delivery Unit，不要再拆成多个独立 Change。

```text
请基于当前项目 SDD 规范，对以下需求执行完整开发流程。

# Requirement

Requirement ID：ENG-M0-001
Requirement Name：Java 后端基础工程
Stage：M0 项目初始化
Priority：P0
Repository：ai-mall-platform（现有 repo-1）
Implementation Scope：backend/**

本 Requirement 作为一个完整 Change 管理。

不要把 Maven、mall-common、mall-contracts、Gateway、服务骨架、MyBatis-Plus、
Flyway、统一响应、异常、TraceId、日志、OpenAPI、测试等再次拆成多个独立 Change。

应在同一个 Change 内通过：

Requirement
→ Exploration
→ PRD
→ Design
→ Tasks
→ Delivery Units
→ Dev
→ Test
→ Evidence
→ Converge

完成整个 Java 后端工程基线。

==================================================
一、需求目标
==================================================

建立 AI 智能电商微服务平台完整的 Java 后端工程基线。

完成后，后续 M1 身份权限、M2 商品库存、M3 商城基础、M4 订单交易等业务需求，
都能够直接在该工程基线上开发，而无需重新处理：

- Java 版本；
- Maven 工程结构；
- Spring Boot / Spring Cloud / Spring Cloud Alibaba 版本；
- 公共模块；
- 服务基础骨架；
- Web 基础协议；
- 数据访问基础；
- 日志与 Trace；
- OpenAPI；
- Java 测试基础。

本 Requirement 的核心不是开发业务，而是建立：

统一工程结构
+
统一版本治理
+
统一公共技术能力
+
统一服务骨架
+
统一开发基础
+
统一构建入口

==================================================
二、已确认技术决策
==================================================

以下决策已经确定，不需要再次讨论或修改：

1. Java

Java 21

2. Maven

Maven 3.9+

不使用 Maven Wrapper。

构建命令统一使用：

mvn ...

而不是：

./mvnw ...

3. Spring 技术基线

Spring Boot：3.5.15
Spring Cloud：2025.0.3
Spring Cloud Alibaba：2025.0.0.0

不得由各业务服务自行覆盖基础框架版本。

4. Repository

项目继续采用 Monorepo。

Java 后端位于：

ai-mall-platform/backend/

不要新建独立 backend Git Repository。

5. mall-common

规划的 8 个模块全部建立：

mall-common-core
mall-common-web
mall-common-security
mall-common-redis
mall-common-mq
mall-common-openfeign
mall-common-log
mall-common-test

本阶段建立模块、边界和必要基础能力。

不得因为模块已经建立就提前完成后续 MQ、RBAC、Redis 业务等功能。

6. mall-contracts

建立：

mall-api-contracts
mall-event-contracts

只用于跨服务通信契约。

7. 微服务骨架

以下服务都必须具有独立 Spring Boot Application 主类：

mall-gateway
mall-identity
mall-member
mall-product
mall-cart
mall-order
mall-inventory
mall-search
mall-system

每个可部署服务必须能够独立打包成 Jar。

M0 不实现具体业务功能。

8. BOM 首批治理范围

mall-bom 负责：

- Spring Boot BOM；
- Spring Cloud BOM；
- Spring Cloud Alibaba BOM；
- MyBatis-Plus；
- MapStruct；
- Springdoc OpenAPI。

Lombok 优先使用 Spring Boot BOM 已管理版本，
不要无必要重复覆盖。

==================================================
三、目标工程结构
==================================================

最终 backend 至少形成：

backend/
├── pom.xml
│
├── mall-bom/
│   └── pom.xml
│
├── mall-common/
│   ├── pom.xml
│   ├── mall-common-core/
│   ├── mall-common-web/
│   ├── mall-common-security/
│   ├── mall-common-redis/
│   ├── mall-common-mq/
│   ├── mall-common-openfeign/
│   ├── mall-common-log/
│   └── mall-common-test/
│
├── mall-contracts/
│   ├── pom.xml
│   ├── mall-api-contracts/
│   └── mall-event-contracts/
│
├── mall-gateway/
│
└── mall-services/
    ├── pom.xml
    ├── mall-identity/
    ├── mall-member/
    ├── mall-product/
    ├── mall-cart/
    ├── mall-order/
    ├── mall-inventory/
    ├── mall-search/
    └── mall-system/

如果当前仓库已经存在上述结构，应基于已有实现补全，不要无理由重建或删除已有代码。

==================================================
四、需求范围
==================================================

本 Requirement 包含以下 6 个能力域。

--------------------------------
A. Maven 与版本治理
--------------------------------

需要完成：

- backend 根 Maven 聚合工程；
- mall-bom；
- Java 21 编译基线；
- Spring Boot 统一版本；
- Spring Cloud 统一版本；
- Spring Cloud Alibaba 统一版本；
- 公共依赖统一版本管理；
- Maven Plugin 统一管理入口；
- 所有 Java 模块加入 Maven Reactor；
- 支持根目录全量构建；
- 支持单模块 -pl -am 构建。

禁止：

- 业务模块自行指定另一套 Spring Boot 版本；
- 业务模块自行维护另一套 Spring Cloud / SCA 版本；
- 大量重复声明已经由 BOM 管理的 dependency version；
- Maven 模块循环依赖。

--------------------------------
B. mall-common 与 mall-contracts
--------------------------------

mall-common 定位：

共享技术能力。

mall-contracts 定位：

跨服务通信契约。

必须保持：

mall-common
=
技术复用

mall-contracts
=
API / Event Published Language

mall-services
=
业务模型和业务规则

mall-common 中禁止出现：

Product
Order
Inventory
Member
Role
Menu
ShoppingCart
RefundRequest

以及具体业务：

Repository
Mapper
DomainService
ApplicationService

mall-contracts 中允许：

- Internal API Request；
- Internal API Response；
- Integration Event；
- Event Metadata；
- 必要的契约基础类型。

mall-contracts 中禁止：

- Aggregate；
- Repository；
- Mapper；
- PO；
- DomainService；
- ApplicationService；
- 业务实现。

不得创建一个 mall-common-all 再让所有服务统一依赖全部 common 模块。

业务服务按需依赖 common。

--------------------------------
C. Gateway 与业务服务骨架
--------------------------------

建立：

mall-gateway

以及：

mall-identity
mall-member
mall-product
mall-cart
mall-order
mall-inventory
mall-search
mall-system

要求：

- 每个服务有独立 Spring Boot Application；
- 每个服务可独立编译；
- 每个服务可独立打包；
- 每个服务生成独立 Jar；
- 服务名称规范统一；
- application 配置结构统一；
- 为后续 Nacos Discovery 接入预留正确结构。

注意：

本 Requirement 不依赖本地 Nacos 必须已经运行。

Nacos 容器和基础设施属于 ENG-M0-004。

因此本 Requirement 内：

允许完成 Nacos 依赖和配置准备；

真正“所有服务成功注册到 Nacos”的集成验证，
在 ENG-M0-004 完成后或 M0 阶段总验收时执行。

禁止在服务骨架中提前开发：

- 登录；
- 商品；
- SKU；
- 库存；
- 购物车；
- 订单；
- 搜索；
- 系统配置等业务逻辑。

--------------------------------
D. 数据访问基础
--------------------------------

完成：

- MyBatis-Plus 接入；
- 通用数据访问基础配置；
- Flyway 接入；
- Flyway migration 目录规范；
- 各服务能够独立维护自己的数据库迁移；
- 数据库连接参数通过配置和环境变量提供。

禁止：

- 把真实密码写入代码或提交 Git；
- 建立跨服务数据库访问；
- 为了验证框架提前创建正式商品、订单、库存等业务表。

如果 ENG-M0-004 MySQL 尚未完成：

本 Requirement 只要求数据访问与 Flyway 工程基线正确；

真实 MySQL 连接和 Migration 集成验证，
可在 M0 总验收中完成。

--------------------------------
E. Web、异常、Trace、日志和 OpenAPI 基础
--------------------------------

建立各 Java HTTP 服务可复用的基础能力。

至少包含：

1. 统一响应基础

响应结构统一包含：

success
code
message
data
traceId

2. 异常处理基础

支持：

- 参数校验异常；
- 通用业务异常；
- 未预期系统异常；
- HTTP 状态与业务错误结构转换。

接口不得直接暴露：

- Java StackTrace；
- SQL；
- 数据库敏感信息；
- Token；
- Password；
- Secret。

3. TraceId

要求：

- 请求缺少 TraceId 时可生成；
- 请求已有合法 TraceId 时可以传递；
- TraceId 写入日志上下文；
- 响应可以返回 TraceId；
- 为后续 Feign / MQ Trace 传播预留能力。

4. 日志

建立统一基础日志配置。

本阶段不要求 Prometheus、Grafana、Loki、SkyWalking 等完整观测平台。

5. OpenAPI

Spring HTTP 服务能够生成或访问 OpenAPI 文档。

不得为业务接口提前创建大量空 Controller。

--------------------------------
F. Java 测试基础
--------------------------------

建立后续 Java 服务统一可使用的测试基础。

至少支持：

- JUnit 5；
- AssertJ；
- Spring Boot Test；
- 基础测试公共能力；
- Maven Test 生命周期。

可建立：

mall-common-test

但不要提前实现具体：

OrderFixture
ProductFixture
InventoryFixture

等业务测试数据。

至少保证基础工程可以执行测试并通过。

==================================================
五、架构约束
==================================================

必须遵守：

1. 不共享业务领域模型

禁止为了减少代码重复，把：

Product
Order
Inventory
Member

抽入 common 或 contracts。

2. 服务间禁止直接 Maven 依赖实现

禁止：

mall-order
→ mall-product implementation

或者：

mall-inventory
→ mall-order implementation

跨服务协作未来使用：

- Internal API；
- OpenFeign；
- Integration Event；
- ACL。

3. mall-common-core 保持最轻量

mall-common-core 不应该无理由依赖：

- Spring Web；
- Redis；
- RocketMQ；
- OpenFeign；
- 业务服务。

4. 不提前实现业务

M0-001 是工程基线 Change。

禁止为了“顺便验证”直接实现：

- 用户注册登录；
- RBAC；
- 商品 CRUD；
- SKU；
- 库存；
- 购物车；
- 下单；
- 支付；
- 搜索。

5. 不提前实现完整分布式增强

不实现：

- Outbox；
- 消息最终一致性；
- RocketMQ 订单业务；
- 延迟订单；
- 消息补偿；
- Elasticsearch；
- RAG。

这些属于后续阶段。

==================================================
六、建议 Task / Delivery Unit 划分
==================================================

该 Requirement 只创建一个 Change。

在 Change 内建议拆为：

TASK-001 Maven 与 BOM 基线
TASK-002 mall-common / mall-contracts
TASK-003 Gateway 与微服务 Skeleton
TASK-004 MyBatis-Plus / Flyway
TASK-005 Web / Response / Exception
TASK-006 Trace / Log / OpenAPI / Test
TASK-007 全量验证与 Evidence

对应 Delivery Unit 可以参考：

DU-BE-001 Maven / BOM
DU-BE-002 Common / Contracts
DU-BE-003 Service Skeleton
DU-BE-004 Persistence Foundation
DU-BE-005 Web Foundation
DU-BE-006 Observability & Test Foundation
DU-BE-007 Verification

Delivery Unit 可以根据最终 Design 调整。

但是：

不要因此创建 7 个独立 Change。

==================================================
七、验收标准
==================================================

AC-01 Java / Maven 基线

满足：

Java = 21
Maven >= 3.9

--------------------------------

AC-02 Maven Reactor

所有规划 Java 模块均进入 Maven Reactor。

不存在：

- 有目录但没有加入 module；
- 父 POM 指向不存在目录；
- module 重复；
- module 无法解析。

--------------------------------

AC-03 全量构建

在 backend/ 执行：

mvn clean package -DskipTests

必须最终：

BUILD SUCCESS

--------------------------------

AC-04 单模块构建

至少验证一个核心服务，例如 mall-order：

mvn clean package -pl mall-services/mall-order -am -DskipTests

必须：

BUILD SUCCESS

--------------------------------

AC-05 统一版本治理

确认：

Java 21

Spring Boot 3.5.15
Spring Cloud 2025.0.3
Spring Cloud Alibaba 2025.0.0.0

由统一版本管理控制。

业务服务不得自行覆盖另一套基础框架版本。

--------------------------------

AC-06 Common 边界

mall-common 中不存在具体业务领域模型和业务服务实现。

--------------------------------

AC-07 Contracts 边界

mall-contracts 中不存在：

Repository
Mapper
PO
Aggregate
DomainService
ApplicationService

--------------------------------

AC-08 服务独立性

mall-gateway 和 8 个业务服务均：

- 有独立 Application 主类；
- 可独立编译；
- 可独立打包；
- 产生独立 Jar。

--------------------------------

AC-09 数据访问基础

MyBatis-Plus 与 Flyway 基础配置正确。

不存在跨服务数据库访问。

--------------------------------

AC-10 Web 基础

通过最小验证方式确认：

- 统一响应可使用；
- 参数异常可转换；
- 系统异常不会暴露敏感内部信息；
- TraceId 能进入响应/日志；
- OpenAPI 能正常工作。

验证代码不得演变成正式业务功能。

--------------------------------

AC-11 测试基础

执行：

mvn test

当前已有基础测试必须通过。

不得通过：

- 删除失败测试；
- @Disabled 隐藏问题；
- 跳过验证后声称 PASS；

完成验收。

--------------------------------

AC-12 依赖方向

不存在：

- Maven 循环依赖；
- 业务服务直接依赖其他业务服务实现；
- mall-common-core 反向依赖大量上层技术模块。

==================================================
八、M0-001 Definition of Done
==================================================

以下全部满足才允许将 ENG-M0-001 标记 DONE：

[ ] Java 21 基线完成
[ ] Maven 3.9+ 基线完成
[ ] backend 根 POM 完成
[ ] mall-bom 完成
[ ] Spring Boot 统一版本完成
[ ] Spring Cloud 统一版本完成
[ ] Spring Cloud Alibaba 统一版本完成
[ ] MyBatis-Plus 等首批公共版本治理完成

[ ] mall-common 8 个模块完成
[ ] mall-contracts 2 个模块完成
[ ] common / contracts 边界正确

[ ] mall-gateway 骨架完成
[ ] mall-identity 骨架完成
[ ] mall-member 骨架完成
[ ] mall-product 骨架完成
[ ] mall-cart 骨架完成
[ ] mall-order 骨架完成
[ ] mall-inventory 骨架完成
[ ] mall-search 骨架完成
[ ] mall-system 骨架完成

[ ] 所有服务有独立 Spring Boot Application
[ ] 所有服务能够独立打包

[ ] MyBatis-Plus 基础完成
[ ] Flyway 基础完成

[ ] 统一响应基础完成
[ ] 异常处理基础完成
[ ] TraceId 基础完成
[ ] 日志基础完成
[ ] OpenAPI 基础完成
[ ] Java 测试基础完成

[ ] 无业务模型污染 mall-common
[ ] 无领域实现污染 mall-contracts
[ ] 无业务服务直接实现依赖
[ ] 无 Maven 循环依赖

[ ] mvn clean package -DskipTests PASS
[ ] mvn test PASS
[ ] 单模块 -pl -am 构建 PASS

[ ] 实际测试结果已经记录
[ ] Evidence 已保存
[ ] 未声称未执行的测试已通过
[ ] 未提前实现 M1 及后续阶段的大量业务能力

==================================================
九、Evidence 要求
==================================================

在当前 Change 中记录实际验证证据。

至少包含：

1. Environment

Java Version
Maven Version

2. Version Baseline

Spring Boot
Spring Cloud
Spring Cloud Alibaba

3. Maven Reactor

实际参与构建的 Module 列表。

4. Full Build

Command：

mvn clean package -DskipTests

Result：

PASS / FAIL

5. Tests

Command：

mvn test

Result：

PASS / FAIL

6. Module Build

例如：

mvn clean package -pl mall-services/mall-order -am -DskipTests

Result：

PASS / FAIL

7. Architecture Check

- mall-common 领域污染检查；
- mall-contracts 实现污染检查；
- 跨业务服务 Maven 依赖检查；
- 循环依赖检查。

8. 未完成事项

如果依赖 ENG-M0-004 才能完成，例如：

- Nacos 实际注册；
- MySQL 实际连接；
- Flyway 对真实 MySQL 执行；

必须明确记录为：

Pending M0 Integration Verification

而不是伪造 PASS。

==================================================
十、非本 Requirement 范围
==================================================

本 Requirement 不负责：

- Docker Compose 基础设施；
- MySQL / Redis / Nacos / MinIO 容器部署；
- mall-web；
- mall-admin；
- ai-service；
- 管理员登录；
- RBAC；
- 商品；
- SKU；
- 库存业务；
- 会员业务；
- 购物车；
- 订单；
- 模拟支付；
- RocketMQ 业务；
- Elasticsearch；
- AI Agent；
- RAG。

分别由：

ENG-M0-002 前端基础工程
ENG-M0-003 AI Service 基础工程
ENG-M0-004 本地基础设施环境

以及后续业务阶段 Requirement 负责。

==================================================
十一、执行要求
==================================================

1. 首先读取当前 Workspace：
   - standards/
   - product/
   - delivery/
   - .sdd/repositories.yaml
   - implementation/repo-1/backend/

2. 如果当前代码已经部分实现：
   不允许直接覆盖或重建。
   先检查 Current State 与 Requirement Gap。

3. Exploration 阶段：
   明确：
   - 已存在什么；
   - 缺少什么；
   - 哪些设计已被仓库实现；
   - 哪些内容与当前标准冲突。

4. Design 必须记录：
   - Maven 层级；
   - BOM 策略；
   - common/contracts 边界；
   - 模块依赖方向；
   - 服务 Skeleton；
   - Web Foundation；
   - Persistence Foundation；
   - Test Foundation；
   - Delivery Unit 划分。

5. Tasks 根据 Design 拆分，
   不要重新创造 Requirement。

6. Dev 阶段只修改 Requirement 范围内代码。

7. Test 必须真实执行。

8. 测试失败时：
   不允许删除测试或伪造结果。

9. 完成后：
   将验证结果保存为 Evidence，
   再执行 Converge。

10. implementation 中的业务代码属于真实实现，
    不得为了整理 SDD 而随意移动、删除或覆盖已有正确实现。

最终目标：

完成 ENG-M0-001 后，

backend 应成为一个：

可统一构建
+
可独立开发微服务
+
版本统一
+
边界清晰
+
具备基本 Web / Persistence / Logging / Testing 能力

的 Java 微服务工程基线，

并可以直接作为 M1 及以后所有 Java 后端业务开发的基础。
```
