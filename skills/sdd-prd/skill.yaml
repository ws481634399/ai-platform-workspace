# OpenSpec SDD Skill 定义（骨架）
#
# 版本: v0.1
# 类型: Skill 元数据 + Artifact Contract
#
# Phase 1.4 仅提供 skill.yaml + 基础 SKILL.md 骨架，不实现完整执行逻辑（后续逐阶段完善）。
# v0.1 由外部 Agent 按 Instruction 执行，产出写回 CHG 目录，再用 openspec change status --set 推进状态。

id: sdd-prd
version: 0.2.0
stage: prd
description: 产品规格阶段 Skill——将需求转化为产品规格，定义业务规则与验收标准
input:
  - exploration.md
  - requirement.md
  - product/
output:
  - prd.md
requires-state: exploring
produces-state: specified
output-artifacts:
  - prd.md
prompts: # Phase 2.3：引用 prompts/ 提示片段（相对 prompts/，不含 .md）
  - common/persona-sdd
  - common/constraints
  - common/output-format
  - explore/persona-prd
