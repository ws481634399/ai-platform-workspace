# AI 智能电商微服务平台 · 本地启动总指南

> 适用阶段：**M0 工程基线**（四个基础需求 CHG-0001 ~ CHG-0006 已完成）
> 操作系统：Windows + PowerShell（5.1 或 7+）
> 更新日期：2026-09-10

---

## 1. 仓库结构总览

本项目是**多仓（multi-repo）**结构。当前工作区 `ai-platform/` 只管理 SDD 文档资产，所有实现代码位于 `implementation/` 下四个相互独立的 Git 仓库：

```text
ai-platform/                         # 工作区仓（repo-workspace，本文档所在位置）
└── implementation/
    ├── ai-platform-infrastructure/  # 本地基础设施（repo-4，Docker Compose）
    │   └── deploy/
    ├── ai-platform-backend/         # Java 后端 Monorepo（repo-1，Maven 根=仓库根）
    ├── ai-platform-frontend/        # 前端仓（repo-2，内含两个完全独立的 Vue3 应用）
    │   ├── mall-web/                #   商城用户端
    │   └── mall-admin/              #   后台管理端
    └── ai-platform-ai-service/      # Python AI 服务（repo-3，FastAPI + uv）
```

启动顺序（依赖方向）：

```text
① 基础设施（Docker）  →  ② Java 后端  →  ③ 前端
                                      ↘  ④ AI Service（依赖网关，但 M0 仅健康检查，可独立启动）
```

---

## 2. 本机环境要求

| 工具           | 版本要求                                                    | 用途                               | 校验命令                 |
| -------------- | ----------------------------------------------------------- | ---------------------------------- | ------------------------ |
| Docker Desktop | 已启动即可                                                  | MySQL / Redis / Nacos / MinIO 容器 | `docker info`            |
| Docker Compose | 随 Docker Desktop 安装                                      | 容器编排                           | `docker compose version` |
| JDK            | **21**（区间 `[21,22)`，enforcer 强制）                     | 后端构建与运行                     | `java -version`          |
| Maven          | **3.9+**（项目不提交 Wrapper，用本机 Maven）                | 后端构建                           | `mvn -version`           |
| Node.js        | **>= 22**                                                   | 前端运行时                         | `node -v`                |
| pnpm           | **>= 10**                                                   | 前端包管理                         | `pnpm -v`                |
| Python         | **3.11**（由 uv 按 `.python-version` 自动安装，无需手动装） | AI 服务                            | —                        |
| uv             | 最新版即可                                                  | AI 服务依赖管理                    | `uv --version`           |

uv 安装（任选其一，未安装时执行）：

```powershell
powershell -ExecutionPolicy ByPass -c "irm https://astral.sh/uv/install.ps1 | iex"
# 或
pip install uv
```

pnpm 安装：

```powershell
npm install -g pnpm
```

---

## 3. 端口与账号总表

### 3.1 基础设施端口（全部在 `deploy/.env` 中可改）

| 服务          | 宿主机地址                | 容器网络地址 | 账号 / 密码                                                               |
| ------------- | ------------------------- | ------------ | ------------------------------------------------------------------------- |
| MySQL 8.4     | `localhost:13306`         | `mysql:3306` | `root` / `123456`；应用账号 `mall_local` / `123456`                       |
| Redis 7.4     | `localhost:6379`          | `redis:6379` | 密码 `123456`                                                             |
| Nacos Server  | `http://localhost:8848`   | `nacos:8848` | 本地 Auth 已关闭                                                          |
| Nacos gRPC    | `localhost:9848`          | `nacos:9848` | —                                                                         |
| Nacos Console | **http://localhost:8849** | `nacos:8080` | 账号 `nacos`，首次访问需初始化管理员密码（本地开发自行设定，如 `123456`） |
| MinIO API     | `http://localhost:9000`   | `minio:9000` | `minio_local` / `12345678`                                                |
| MinIO Console | **http://localhost:9001** | `minio:9001` | `minio_local` / `12345678`                                                |

容器统一加入 Docker 网络 `ai-platform-network`，数据使用命名卷持久化（`ai-platform-mysql-data` 等）。

### 3.2 应用端口

| 应用                  | 端口                      | 说明                            |
| --------------------- | ------------------------- | ------------------------------- |
| mall-gateway          | **8080**                  | Spring Cloud Gateway 统一入口   |
| mall-identity         | 8101                      | 身份认证服务                    |
| mall-member           | 8102                      | 会员服务                        |
| mall-product          | 8103                      | 商品服务                        |
| mall-cart             | 8104                      | 购物车服务                      |
| mall-order            | 8105                      | 订单服务                        |
| mall-inventory        | 8106                      | 库存服务                        |
| mall-search           | 8107                      | 搜索服务                        |
| mall-system           | 8108                      | 系统配置服务                    |
| mall-web（Vite）      | **5173**                  | 商城用户端 dev server           |
| mall-admin（Vite）    | 5173（被占用时自动 5174） | 后台管理端 dev server           |
| ai-service（uvicorn） | **8000**                  | FastAPI，健康检查 `GET /health` |

> 前端通过 Vite proxy 把 `/api` 转发到 `http://localhost:8080`（mall-gateway），开发期无跨域问题。

---

## 4. 第一步：启动基础设施

目录：`implementation/ai-platform-infrastructure/deploy/`

```powershell
cd implementation/ai-platform-infrastructure/deploy

# 1) 首次使用：复制环境变量文件（.env 不提交 Git，仅本机使用）
Copy-Item .env.example .env

# 2) 一键启动四个容器（后台运行）
./scripts/infra.ps1 -Action start

# 3) 等待全部 healthy（Nacos 首次启动较慢，超时默认 180s）
./scripts/infra.ps1 -Action health

# 4) 查看状态 / 日志
./scripts/infra.ps1 -Action status
./scripts/infra.ps1 -Action logs -Service nacos
```

等价原生命令（脚本只是封装）：

```powershell
docker compose --env-file .env -f docker-compose.infra.yml up -d
docker compose --env-file .env -f docker-compose.infra.yml ps
```

首次创建空 MySQL 卷时，初始化脚本会自动创建 8 个空库：`mall_identity`、`mall_member`、`mall_product`、`mall_cart`、`mall_order`、`mall_inventory`、`mall_search`、`mall_system`。**只创建空库，不建业务表**（业务表后续由 Flyway 管理）。

> 若提示脚本无法执行（执行策略限制）：
> `powershell -ExecutionPolicy Bypass -File ./scripts/infra.ps1 -Action start`

---

## 5. 第二步：启动 Java 后端

目录：`implementation/ai-platform-backend/`

### 5.1 构建

```powershell
cd implementation/ai-platform-backend

# 全量构建 24 个 Maven 模块（跳过测试）
mvn clean package -DskipTests

# 或只构建单个服务（-am 同时构建其依赖的 common/bom）
mvn clean package -DskipTests -pl mall-services/mall-identity -am
```

### 5.1.1 准备 JWT 密钥（mall-identity 必需）

`mall-identity` 签发/校验 Token 需要 RSA 密钥对。`application.yml` 中 `JWT_PUBLIC_KEY_LOCATION` / `JWT_PRIVATE_KEY_LOCATION` **无默认值**，不提供密钥会导致 `jwtEncoder` Bean 创建失败、服务无法启动。

在后端仓根目录生成密钥（本机无 openssl，用 Docker 镜像生成）：

```powershell
cd implementation/ai-platform-backend

# 生成本地密钥目录
New-Item -ItemType Directory -Path local-keys -Force

# 生成 RSA 2048 密钥对（公钥 X.509 PEM / 私钥 PKCS8 PEM）
docker run --rm -v "${PWD}/local-keys:/keys" alpine/openssl genpkey -algorithm RSA -out /keys/jwt-private.pem -pkeyopt rsa_keygen_bits:2048
docker run --rm -v "${PWD}/local-keys:/keys" alpine/openssl rsa -pubout -in /keys/jwt-private.pem -out /keys/jwt-public.pem
```

生成后 `local-keys/` 下有 `jwt-private.pem` 和 `jwt-public.pem`。该目录已加入 `.gitignore`（含私钥，禁止提交）。

> 若本机已装 openssl，可直接用 `openssl genpkey` / `openssl rsa -pubout` 生成，无需 Docker。

### 5.2 启动方式 A：裸启动（不依赖基础设施）

默认配置下 `NACOS_ENABLED=false`、`FLYWAY_ENABLED=false`，数据库连接池懒加载，**不起 Docker 也能把服务跑起来**（仅验证工程可启动）：

```powershell
# 网关
java -jar mall-gateway/target/mall-gateway-1.0.0-SNAPSHOT.jar

# 业务服务（新开一个 PowerShell 窗口）
java -jar mall-services/mall-identity/target/mall-identity-1.0.0-SNAPSHOT.jar
```

也可以直接在 IDEA / VS Code 中运行各模块的主类（如 `MallIdentityApplication`），无需额外参数。

### 5.3 启动方式 B：联调启动（连接基础设施 + 注册 Nacos）

先完成第 4 步。**推荐使用仓库根的本机专用脚本**（已 gitignore，自动注入 MySQL / Nacos / JWT 密钥等环境变量与服务映射）：

```powershell
cd implementation/ai-platform-backend
.\start-dev.ps1 identity      # 启动 mall-identity（自动注入下方环境变量）
.\start-dev.ps1 list          # 查看全部 9 个可启动服务
```

手动方式（等价）：在**启动服务的 PowerShell 窗口**中设置环境变量：

```powershell
$env:MYSQL_USER = "mall_local"
$env:MYSQL_PASSWORD = "123456"
$env:MYSQL_PORT = "13306"   # 本机 3306 被原生 MySQL 占用，Docker MySQL 映射在 13306（见 FAQ Q2）
$env:NACOS_ENABLED = "true"
$env:NACOS_ADDR = "localhost:8848"
# mall-identity 还需 JWT 密钥路径（见 5.1.1）
$env:JWT_PUBLIC_KEY_LOCATION  = "file:$PWD/local-keys/jwt-public.pem"
$env:JWT_PRIVATE_KEY_LOCATION = "file:$PWD/local-keys/jwt-private.pem"

java -jar mall-services/mall-identity/target/mall-identity-1.0.0-SNAPSHOT.jar
```

- 各服务数据库名通过 `MYSQL_DB_*` 变量覆盖，但默认值已与空库一一对应（如 `MYSQL_DB_IDENTITY=mall_identity`），**通常无需设置**；
- `MYSQL_HOST` 默认 `localhost`、`MYSQL_PORT` 默认 `3306`；**本机 3306 被原生 MySQL 占用时必须设为 `13306`**（见 FAQ Q2）；
- 网关只读取 `NACOS_*`，设置上述变量无副作用。

验证注册：启动后打开 Nacos Console http://localhost:8849 → 服务管理中能看到对应实例。

### 5.4 批量启动全部 9 个应用（可选）

```powershell
$env:MYSQL_USER = "mall_local"
$env:MYSQL_PASSWORD = "123456"
$env:MYSQL_PORT = "13306"
$env:NACOS_ENABLED = "true"
$env:NACOS_ADDR = "localhost:8848"
$env:JWT_PUBLIC_KEY_LOCATION  = "file:$PWD/local-keys/jwt-public.pem"
$env:JWT_PRIVATE_KEY_LOCATION = "file:$PWD/local-keys/jwt-private.pem"

$apps = @(
  @{ Name = "mall-gateway";   Jar = "mall-gateway/target/mall-gateway-1.0.0-SNAPSHOT.jar" },
  @{ Name = "mall-identity";  Jar = "mall-services/mall-identity/target/mall-identity-1.0.0-SNAPSHOT.jar" },
  @{ Name = "mall-member";    Jar = "mall-services/mall-member/target/mall-member-1.0.0-SNAPSHOT.jar" },
  @{ Name = "mall-product";   Jar = "mall-services/mall-product/target/mall-product-1.0.0-SNAPSHOT.jar" },
  @{ Name = "mall-cart";      Jar = "mall-services/mall-cart/target/mall-cart-1.0.0-SNAPSHOT.jar" },
  @{ Name = "mall-order";     Jar = "mall-services/mall-order/target/mall-order-1.0.0-SNAPSHOT.jar" },
  @{ Name = "mall-inventory"; Jar = "mall-services/mall-inventory/target/mall-inventory-1.0.0-SNAPSHOT.jar" },
  @{ Name = "mall-search";    Jar = "mall-services/mall-search/target/mall-search-1.0.0-SNAPSHOT.jar" },
  @{ Name = "mall-system";    Jar = "mall-services/mall-system/target/mall-system-1.0.0-SNAPSHOT.jar" }
)
foreach ($app in $apps) { Start-Process -FilePath java -ArgumentList "-jar", $app.Jar }
```

> 当前 M0 阶段保持 `FLYWAY_ENABLED=false`：各服务 `db/migration` 目录尚无迁移脚本。

---

## 6. 第三步：启动前端

两个应用**完全独立**（各自 package.json / lockfile，非 workspace），分别安装与启动。

### 6.1 商城用户端 mall-web

```powershell
cd implementation/ai-platform-frontend/mall-web
pnpm install
pnpm dev
# 访问 http://localhost:5173
```

### 6.2 后台管理端 mall-admin

```powershell
cd implementation/ai-platform-frontend/mall-admin
pnpm install
pnpm dev
# 若 5173 已被 mall-web 占用，Vite 会自动改用 5174；也可显式指定：
# pnpm dev --port 5174
```

其他常用命令（两个应用相同）：

```powershell
pnpm lint        # ESLint
pnpm type-check  # vue-tsc 类型检查
pnpm build       # 生产构建 → dist/
pnpm preview     # 预览构建产物
```

npm 源已在各应用 `.npmrc` 中统一配置为 `https://registry.npmmirror.com`，通常无需额外换源。

---

## 7. 第四步：启动 AI Service

目录：`implementation/ai-platform-ai-service/`

```powershell
cd implementation/ai-platform-ai-service

# 1) 按 lockfile 安装依赖（uv 自动按 .python-version 准备 Python 3.11）
uv sync --frozen

# 2) 可选：复制环境变量（不复制也能用默认值启动）
Copy-Item .env.example .env

# 3) 启动开发服务（热重载）
uv run uvicorn app.main:app --reload --log-level info

# 4) 健康检查
curl http://127.0.0.1:8000/health
# 期望返回：{"service":"ai-service","status":"UP"}
```

`.env` 关键默认值：`LLM_PROVIDER=mock`（不依赖真实大模型 Key）、`JAVA_API_BASE_URL=http://localhost:8080`（指向网关，M0 尚无实际业务调用）。

---

## 8. 启动后验证清单

| #   | 验证项        | 方式                                                     | 期望结果                |
| --- | ------------- | -------------------------------------------------------- | ----------------------- |
| 1   | 四个容器      | `./scripts/infra.ps1 -Action health`（在 deploy 目录）   | 全部 healthy            |
| 2   | MySQL 连通    | 客户端连 `localhost:13306`，账号 `mall_local/123456`     | 看到 8 个 `mall_*` 空库 |
| 3   | Redis         | `docker exec -it <redis容器> redis-cli -a 123456 ping`   | `PONG`                  |
| 4   | Nacos Console | 浏览器打开 http://localhost:8849                         | 页面可访问              |
| 5   | MinIO Console | 浏览器打开 http://localhost:9001，`minio_local/12345678` | 可登录                  |
| 6   | Java 服务注册 | 服务按方式 B 启动后查看 Nacos 服务列表                   | 出现对应实例            |
| 7   | 网关端口      | `curl http://localhost:8080`（无路由属正常，能连通即可） | 有响应/连接成功         |
| 8   | mall-web      | http://localhost:5173                                    | 商城骨架页              |
| 9   | mall-admin    | http://localhost:5173（或 5174）                         | 后台登录骨架页          |
| 10  | AI 服务       | http://127.0.0.1:8000/health                             | `{"status":"UP"}`       |

---

## 9. 停止与清理

```powershell
# 基础设施：停止并删除容器，但保留数据卷（普通 down，数据不丢）
cd implementation/ai-platform-infrastructure/deploy
./scripts/infra.ps1 -Action stop

# Java 后端：在对应窗口 Ctrl+C；批量启动的进程可用：
Get-Process -Name java -ErrorAction SilentlyContinue | Stop-Process

# 前端 / AI 服务：各自终端 Ctrl+C 即可
```

**危险操作（会永久删除本地数据，脚本刻意不提供封装）**：

```powershell
docker compose --env-file .env -f docker-compose.infra.yml down --volumes
```

---

## 10. M0 阶段能力边界（重要）

当前为**工程基线**，目标是"可构建、可启动、可联调基础设施"，不含业务功能：

- **后端**：9 个应用是可独立编译/打包/启动的骨架，暂无业务接口与业务表；服务间禁止 Maven 互依；
- **前端**：mall-web / mall-admin 仅有布局、登录页、工作台等占位页面；
- **AI Service**：仅提供 `/health` 与 LLM mock provider，agents / rag / tools / workflows 为 M6 预留空包；
- **Java → Redis 尚未接通**：业务服务还未引用 `mall-common-redis`，该项在 CHG-0006 验收中记录为 PENDING（容器 PING 正常不等于 Java 客户端已接通）；
- 基础设施**不含** RocketMQ、Elasticsearch、向量库、Prometheus/Grafana/Loki、SkyWalking、Nginx，后续里程碑按需引入；
- 所有 `123456` 类弱口令仅限本机隔离开发，禁止照搬到共享或生产环境；`.env` 不得提交 Git。

---

## 11. 常见问题

**Q1：`infra.ps1` 提示无法加载脚本 / 数字签名？**
执行策略限制。用 `powershell -ExecutionPolicy Bypass -File ./scripts/infra.ps1 -Action start`，或先执行 `Set-ExecutionPolicy -Scope Process Bypass`。

**Q2：3306 / 8848 等端口被本机其他程序占用？**
编辑 `deploy/.env` 中对应 `*_PORT` 变量后重新 `start`；不要改 compose 文件与 README 契约。应用侧同步改 `MYSQL_PORT` / `NACOS_ADDR` 等环境变量。

**Q3：Nacos 容器一直 unhealthy？**
Nacos 3.0.3 首次启动约需 30~60 秒（健康检查 `start_period: 60s`）。用 `./scripts/infra.ps1 -Action logs -Service nacos` 看日志，等 `infra.ps1 -Action health` 轮询通过即可。

**Q4：改了 MySQL 初始化脚本为什么没生效？**
`mysql/init/001-init-databases.sh` 只在**空数据卷首次启动**时执行一次，普通 restart 不会重放。需要重放时先 `down --volumes`（会删数据）。

**Q5：Maven 构建报 JDK 版本错误？**
enforcer 强制 Java 21。检查 `mvn -version` 与 `JAVA_HOME`，切换到 JDK 21 后重试。

**Q6：pnpm install 失败 / 很慢？**
确认 Node >= 22、pnpm >= 10；源已指向 npmmirror，必要时 `pnpm store prune` 后重试。

**Q7：前端页面请求报 502 / 代理失败？**
Vite proxy 目标 `http://localhost:8080` 是 mall-gateway。M0 网关尚无业务路由，需先启动网关；后端在线后再调接口。

**Q8：mall-identity 启动报 `jwtEncoder` / `JwtConfiguration` 创建失败？**
缺少 JWT RSA 密钥对。`application.yml` 中 `JWT_PUBLIC_KEY_LOCATION` / `JWT_PRIVATE_KEY_LOCATION` 无默认值。按 §5.1.1 生成密钥到 `local-keys/`，并确保 `start-dev.ps1` 或手动环境变量指向这两个文件。

---

## 12. 参考文档

- 基础设施详细说明：[deploy/README.md](file:///d:/Desktop/ai-platform/implementation/ai-platform-infrastructure/deploy/README.md)
- 后端说明：[ai-platform-backend/README.md](file:///d:/Desktop/ai-platform/implementation/ai-platform-backend/README.md)
- 前端说明：[ai-platform-frontend/README.md](file:///d:/Desktop/ai-platform/implementation/ai-platform-frontend/README.md)
- AI 服务说明：[ai-platform-ai-service/README.md](file:///d:/Desktop/ai-platform/implementation/ai-platform-ai-service/README.md)
- 本地基础设施需求：[REG-M0-004.md](file:///d:/Desktop/ai-platform/docs/需求/M0/REG-M0-004.md)
- 项目总览：[00-项目总览.md](file:///d:/Desktop/ai-platform/docs/项目文件/00-项目总览.md)
